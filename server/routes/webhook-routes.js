const express = require('express');
const router = express.Router();
// Removed Course model import if not needed for other webhooks
// Removed crypto import if not needed for other webhooks

// Removed AssemblyAI webhook signature verification middleware
// Removed Cloudinary transcription webhook route
// Removed AssemblyAI transcription webhook route

// Add any other webhook routes here if needed

module.exports = router; 